version: '3.8'

services:
  mysql:
    image: mysql:8.0.35
    ports:
      - 3306
    environment:
      MYSQL_DATABASE: swapswap
      MYSQL_ROOT_PASSWORD: password

  redis:
    image: redis:6
    ports:
      - 6379
